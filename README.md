# Populate-the-Lab
PowerShell scripts to build and populate a realistic on-prem Active Directory lab: OU structure, department security groups, random users (from name lists), resource/shared accounts, service accounts, and pre-staged workstation objects for end-to-end GPO testing.
